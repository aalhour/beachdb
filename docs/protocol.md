# Server protocol (TODO)

Season 1 milestone: server mode with a small binary protocol.

This doc will define:

- framing (length-prefixed)
- request/response types
- timeouts and backpressure behavior
- error codes
