# Table layer (TODO / Season 2)

This is not part of BeachDB Season 1.

This doc will define:

- key encoding schemes (row/family/qualifier/ts)
- scan semantics
- delete marker semantics (eventual)
- how it maps onto the KV engine
